# Various UI elements for React

This is boilerplate code to use for projects.

## MUI 5 elements

This is mostly MUI components with a few React components that seem helpful (ex: date picker and color picker)
